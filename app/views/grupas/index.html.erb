<div class="hero-unit">
  

<h1>Spis grup</h1>

<table class ="table table-striped">
  <thead>
    <tr class="info">
      <th>Kurs</th>
      <th>Uzytkownik</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grupas.each do |grupa| %>
      <tr>
        <td><%= grupa.kurs_id %></td>
        <td><%= grupa.uzytkownik_id %></td>
        <td><%= link_to 'Show', grupa , :class => 'btn btn-mini btn-default'%></td>
        <td><%= link_to 'Edit', edit_grupa_path(grupa) , :class => 'btn btn-mini btn-primary'%></td>
        <td><%= link_to 'Destroy', grupa, method: :delete, data: { confirm: 'Are you sure?' } , :class => 'btn btn-mini btn-danger'%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Grupa', new_grupa_path , :class => 'btn btn-mini btn-primary'%>
  <%= link_to("Powrót", {:controller => 'dostep'}, :class => 'btn btn-warning')%>
  </div>
