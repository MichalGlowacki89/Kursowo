<h1>Listing zamowienies</h1>

<table>
  <thead>
    <tr>
      <th>Uzytkownik</th>
      <th>Wartosc w zl</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @zamowienies.each do |zamowieny| %>
      <tr>
        <td><%= zamowieny.uzytkownik_id %></td>
        <td><%= zamowieny.wartosc_w_zl %></td>
        <td><%= link_to 'Show', zamowieny %></td>
        <td><%= link_to 'Edit', edit_zamowieny_path(zamowieny) %></td>
        <td><%= link_to 'Destroy', zamowieny, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Zamowienie', new_zamowieny_path %>
<%= link_to("Powrót", {:controller => 'dostep'}, :class => 'btn btn-default')%>
